> for ln -s, use absolute path general, but for some special conditions is not necessary

<hr> 

> if you want to ln a directory to some where, just jump target use cd, and execute command, such as

```bash
ln ../../plugins ./plugins
```